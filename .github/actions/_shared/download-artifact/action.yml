name: download-artifact
description: Downloads a build artifact from a previous workflow run.
inputs:
  ARTIFACT_FILENAME:
    required: true
  CICD_WORKFLOW:
    required: true
runs:
  using: composite
  steps:
    - uses: dawidd6/action-download-artifact@v21
      with:
        name: ${{ inputs.ARTIFACT_FILENAME }}
        name_is_regexp: true
        skip_unpack: true
        workflow: ${{ inputs.CICD_WORKFLOW }}
