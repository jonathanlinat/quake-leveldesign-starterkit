name: cache-save
description: Saves a cache entry identified by key and path.
inputs:
  CACHE_KEY:
    required: true
  PATH:
    required: true
runs:
  using: composite
  steps:
    - uses: actions/cache/save@v5
      with:
        key: ${{ inputs.CACHE_KEY }}
        path: ${{ inputs.PATH }}
