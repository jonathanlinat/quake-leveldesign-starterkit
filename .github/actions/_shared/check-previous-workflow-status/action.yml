name: check-previous-workflow-status
description: Validates that the previous workflow run completed successfully.
runs:
  using: composite
  steps:
    - run: |
        set -euo pipefail
        echo "Previous workflow succeeded"
      shell: bash
